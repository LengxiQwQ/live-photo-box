# P4-R1 正式能力与架构基线

> 本轮取证身份：HEAD `47c6374412497af0f326a3eb76d1342017840393`，Roadmap SHA-256 `317465937A08A908F318ED0F1005AFF7D5B41E637DFF75276F7C08732BB859A7`。当前用户明确纠正：此前 R1 未正式开始。本文件使用本轮重新读取的源码、重新执行的构建/测试和真实 MSIX 清单；旧 AI 草稿不作验收证据。R1 不修改 production architecture。完整命令见 `.ai/tasks/P4-R1-capability-baseline/EVIDENCE.md`。

## 1. 当前 production 数据流

| 功能 | 真实调用路径 | 数据面与尚未收口的路径 |
|---|---|---|
| Source Inspector | `Media/Inspection/SourceInspector.cs` → `NativeMediaService.InspectMediaWithPlanAsync` → C ABI `lpb_inspect_media_with_plan`（`media_api.cpp:122`）→ `media_inspector.cpp`、协议/容器 parser | Native 判定来源结构、配对、range 和 residue；C# 映射事实。Apple 双文件 UUID、Google V1/V2 XMP、OPPO/vivo item、华为/荣耀 LIVE_、Samsung SEF/mpvd 按仓库厂商协议与样本区分。 |
| Extractor | `NeutralMediaService.cs` → `SourceExtractor.cs` → `NativeMediaService.ExtractMediaAsync` → `lpb_extract_media_with_plan_outputs_v2`（`media_api.cpp:541`）→ `media_extractor.cpp` | Native 持 plan、源/目标 handle、exact extraction、verify/commit/rollback；Managed 编排 workspace/bindings。 |
| Cleaner | `NeutralMediaService.cs` → `SourceProtocolCleaner.cs` → `NativeCleanService.CleanSourceProtocolWithCleanupPlanAsync` → `lpb_issue_cleanup_plan` / `lpb_clean_source_protocol_with_cleanup_plan`（`media_api.cpp:896,1174`）→ `media_cleaner.cpp` 和各协议 sink | P1–P3 authority、artifact identity、handle-first publish、preservation 是 correctness-critical。旧 plan-only ABI 在 `media_api.cpp:840,867` fail closed，不把导出存在误认生产旁路。 |
| Native image/video converter | `Media/Image/ImageConverter.cs`、`Media/Video/VideoConverter.cs` → `NativeMediaService` → `lpb_convert_image` / `lpb_probe_video` / `lpb_remux_video` / `lpb_transcode_video`（`media_api.cpp:1301–1339`）→ `image_converter.cpp` / `video_converter.cpp` | 同容器图片直拷、跨容器 WIC；视频结构 probe/remux 自研、转码 MF。当前固定 temp 与 pathname `MoveFileExW(REPLACE_EXISTING)` 是 R2/R5/R6 的明确风险，不属于可跳过的 UI 功能。 |
| 正式 HEIC/HDR 旁路 | `LivePhotoBatchRunnerService.cs:132` → `HeicConverterService.ConvertToJpegAsync`；`LivePhotoMergeService` 也用 HEIC 转换/写入 → Native image conversion，失败后 `ConvertWithMagickNET`（`HeicConverterService.cs:255,279`）；HDR 走 `StandardHdrConversionService` MagickImage | managed Magick 像素处理会改变正式产物，Native 失败后回退以及 HDR 失败后 `gain map dropped`（`HeicConverterService.cs:115,218`）必须由 R5 做显式能力/保真契约，不能归类为 UI-only。heif-enc/heif-dec helper 当前直接抛未支持。 |
| 目标协议合成 | `LivePhotoBatchRunnerService` → `LivePhotoCompositionService.WriteLivePhotoAsync` → `LivePhotoMergeService.WriteLivePhotoCoreAsync`（`LivePhotoMergeService.cs:240`）→ C# protocol branches 与 FileStream 写入 | 这是 P8/P9 尚待迁移的目标 writer 路径；R1 如实标为 managed result-affecting legacy path，不以 P4-R1 提前重构。 |
| UI-only thumbnail/preview | GUI `ThumbnailService`、`MagickHeicThumbnailProvider`、`MagicScalerHeicThumbnailProvider`、Edit preview | 只影响显示，允许留 Windows/managed；与上面的正式 Magick fallback 分别盘点。 |

## 2. P4 capability inventory

“owner/backend”写当前实现；缺失能力明确写“尚无”，不把 P4 默认库视为已经部署。平台/依赖栏同时记录重复能力和 preservation/correctness 风险。

| Capability | Current owner / backend | 影响正式结果 | 平台耦合、runtime/重复与风险 | R2–R7 |
|---|---|---:|---|---|
| JPEG marker/container truth | Native `metadata/jpeg.cpp`、`media_inspector.cpp`，project-owned parser | 是 | parser 自研；经 `internal.h` 传递带 Win32 I/O，结构 authority 必须基于合法 marker。 | R2/R4 |
| JPEG decode/encode | Native `image_converter.cpp` WIC；managed HEIC fallback Magick.NET 也编码 JPEG | 是 | Windows codecs + Magick 双结果路径，metadata/HDR outcome 不能由 decoder 成功推断。 | R4/R5 |
| JPEG lossless transform | 当前无 libjpeg-turbo transform foundation；仅有旧 `jpeg_structure_cleaner.cpp` 结构 helper | 目标能力 | 尚无正式 lossless codec backend；R4 建立，不用像素重编码冒称 lossless。 | R4 |
| TIFF/EXIF/XMP/MakerNote | Native `metadata/exif.cpp`、`exif_rewrite.cpp`、`protocols/apple.cpp`；managed Apple/HEIF 修整仍在 | 是 | 自研协议语义；Apple ContentIdentifier 和非 live Exif preservation 不能被 codec 替代。 | R2/R4/R5 |
| HEIF/ISO-BMFF item graph | Native `containers/heif.cpp`、`isobmff.cpp` | 是 | project-owned 合法 box hierarchy/item graph；通过 `internal.h` 间接耦合 Windows。 | R2/R5/R7 |
| HEIC decode/encode | Native WIC；正式失败回退 managed Magick/libheif | 是 | 目前未在 Native 建立 libheif/libde265/x265 gateway；发行、重复 decoder 和真实兼容性在 R5 证明。 | R5 |
| HEIF auxiliary/GainMap | Native item 枚举/重组；C# `StandardHdrConversionService` | 是 | embedded/detached/materialized ownership 必须唯一；不能把 GainMap 当 live residue 清除。 | R5 |
| MOV/MP4/SEF container truth | Native `isobmff.cpp`、`mp4_strip.cpp`、`samsung_sef.cpp` | 是 | project-owned；`mp4_strip.cpp` 的 Win32 publish 是 C 类保留区，不能拿字符串/box-type 命中当关系。 | R2/R6 |
| Video structural probe | Native `video_converter.cpp:85` | 是 | 自研 box/track 与 Windows file I/O 同 TU；不由 MF/ffprobe 提供厂商结构 authority。 | R2/R6 |
| Generic MOV/MP4 remux | Native `video_converter.cpp:374` | 是 | 自研 generic remux，当前 pathname publish；时序、音频和旋转须独立 evidence。 | R2/R6 |
| H264/HEVC transcode | Native `video_converter.cpp:555` Media Foundation | 是 | Windows MF；R6 对比最小 libav* / hybrid，并冻结 Windows P5 backend。 | R6 |
| Audio | Native video probe/remux/transcode + MF | 是 | 目标文件音轨结果须实际检验，不以 container 成功替代。 | R6 |
| Orientation | Native Inspector/video facts + C# mapping/HEIF 修整 | 是 | Exif、HEIF transform、视频 matrix 表示不同；Neutral 只能有一套视觉语义。 | R5/R6 |
| ICC/color | WIC/Magick、HEIF colr/nclx 修整 | 是 | 尚无已证明必须 lcms2 的 transform；silent color loss/补 nclx warning 需判定。 | R5 |
| HDR/GainMap pixel processing | C# `StandardHdrConversionService` Magick.NET | 是 | managed result-affecting 且有丢 GainMap 回退，R5 Native foundation 与 before/after preservation gate。 | R5 |
| SHA-256 | Native `foundation/sha256.cpp` project-owned algorithm | 是 | 算法 portable；`sha256.h:30` 的 `HANDLE` 文件签名是平台污染。 | R2/R7 |
| Filesystem/random access | Native `internal.h`、`context.cpp`、Inspector、Extractor/Cleaner、media converters；C# workspace | 是 | Win32 文件身份、handle 和随机访问分散；R2 小 PlatformFilesystem seam。 | R2 |
| Temp/transaction/atomic publish | Native Extractor/Cleaner dedicated sinks + managed commit orchestration | 是 | P1–P3 handle-first create/flush/rename/rollback 已承担 authority；converter 固定 temp/replace 另有风险。 | R2 |
| UI-only thumbnail/preview | GUI Magick/MagicScaler/WIC/Win2D | 否 | 当前正式 Magick path 同包复用，但 UI 路径不能为正式 codec correctness 背书。 | R1 分类完成 |

## 3. Windows contamination map

| 类别 | 当前真实位置与证据 | 后续边界 |
|---|---|---|
| A 合法 Windows backend | `image_converter.cpp` 的 WIC/COM；`video_converter.cpp` 的 MF；`context.cpp` 的 Windows random/file identity 能力 | Windows x64 当前合法，R5/R6 决定媒体 backend 去留；R2 将 filesystem 放在小 adapter 后。 |
| B 意外 portable-core 污染 | `sha256.h/.cpp` 直接使用 `HANDLE`/CreateFileW；`media_inspector.cpp:702`、`preservation_observation.cpp:47` 的 CreateFileW；`containers/heif.cpp`/部分 protocol parser 经 `internal.h` 间接包含 windows.h | 将结构/哈希/观察语义与 I/O 适配分开；R7 用独立 portable-core build 证明，不能更改协议 truth。 |
| C correctness-critical P1–P3 plumbing | `media_extractor.cpp` 源/目标 HANDLE、File ID、flush、handle rename、verify/commit/rollback；`media_cleaner.cpp`、`mp4_strip.cpp`、`heif_cleaner.cpp`、`samsung_sef_cleaner.cpp`、`foundation/internal.h` 的 create/write/flush/publish/identity/disposition | R2 先包现有安全实现再迁 plumbing。不能 pathname reopen 创造 ownership，不能以 pathname publish/delete 破坏 transaction。 |
| 单列待核的输出风险 | `image_converter.cpp:26,242`、`video_converter.cpp:547,901` 固定 temp/`MoveFileExW(REPLACE_EXISTING)`；`media_api.cpp:1488` 失败 `DeleteFileW`；旧 `jpeg_structure_cleaner.cpp:50` `MoveFileExW` | R2/R5/R6 查真实 caller、authority 和 output role 后处理。旧 utility 不能形成生产绕过。 |

MSVC-only helper 与平台错误码也必须随 R2/R7 复查：`media_extractor.cpp:1146,1229,1278` 将 Win32 disk-full error 映射给正式失败类别，属 Windows backend 错误映射；不能搬进 portable protocol truth。分类基于调用职责，不以 Windows.h 次数判错。

## 4. dependency/runtime、build/ABI/package baseline

| 项 | 本轮重新取得的基线 |
|---|---|
| 构建权威 | 源仓仅 `LivePhotoBox.Native.vcxproj`；`scripts/native/build-native.ps1` 经 VS MSBuild 构建 x64 Debug/Release。无源 `CMakeLists.txt`、CMake presets 或 vcpkg manifest；旧 `artifacts/native/build/` 生成物不是 canonical 源。 |
| Native system imports | `.cpp` 链接 `bcrypt.lib`、`windowscodecs.lib`/`ole32.lib`、`mfplat.lib`/`mfreadwrite.lib`/`mfuuid.lib`；Release DLL `dumpbin /dependents` 实测 KERNEL32、ole32、OLEAUT32、bcrypt、MFPlat、MFReadWrite。Native DLL 未 import 正式 libjpeg-turbo/libheif。 |
| Managed image runtime | Core/GUI csproj 引用 Magick.NET-Q16-x64 14.16.0、MagicScaler 0.15.0、NativeCodecs.Libheif 1.19.5-preview1；Magick 同时服务 UI thumbnail 与正式 HEIC/HDR 结果。R5 要依真实 caller 做去重，不凭包名删除。 |
| External media CLI | Core/GUI/CLI `.cs` 中外部媒体 `.exe` 字面调用没有命中；当前 `Process.Start` 位于自更新、Explorer、设置/关于等非正式媒体流。`LivePhotoRepairService.RunExifToolAsync`、heif-enc/heif-dec helper 均直接抛未支持。MSIX 中 `Assets/Credits/exiftool.png`、`ffmpeg.png` 只是图片，不能当 executable。 |
| C ABI | `include/livephotobox_native.h` 对外为 C ABI/POD/opaque context/显式缓冲区，Managed RequiredAbiVersion=5；本轮 Release DLL `dumpbin /exports` 实测 59 个 `lpb_*`。导出存在与是否准许生产 mutation 分开核查。 |
| Debug Native artifact | 5,513,728 B，SHA-256 `E09474A73EDAE34E07214238CD454EEBD6645650B1429A9D69DC160C1BE14362`。Debug 测试 assembly 目录 DLL 同长同哈希，`NativeMethods` 使用 AssemblyDirectory loading。 |
| Release Native artifact | 915,456 B，SHA-256 `865BFD44B7314D60EC278270E372097ABBF502B59FEABD355BF66CC8FDAD6659`。Release 测试 assembly 目录 DLL 同长同哈希。 |
| Version | 本轮 Core/GUI 构建从 `Package.appxmanifest` 报 `2.2.2.0`；Native `.vcxproj` 从同 manifest 生成 product version header。Release Native runtime tests 15/15 pass。 |
| 当前产品包 | 本轮 `dotnet build LivePhotoBox/LivePhotoBox.csproj -c Release -p:SkipNativeBuild=true` 产出 `LivePhotoBox_2.2.2.0_x64.msix`：118,928,770 B，SHA-256 `1377A86581E2A7143A428759C663C2F22AF2EA6BF3877D8CE56F3F017C7FFF9E`；Zip 包 625 entries、uncompressed 282,439,820 B。包内 `LivePhotoBox.Native.dll` 915,456 B、`heif.dll` 3,152,384 B、`Magick.Native-Q16-x64.dll` 23,949,488 B、PhotoSauce DLL；此为 R3/R7 parity/去重基线，不把旧 setup.exe 算作本 HEAD。 |

## 5. P1–P3 regression 与 RealSample corpus

| Gate | 本轮实测 | 限制 |
|---|---|---|
| Native x64 | Debug 与 Release 各一次 `scripts/native/build-native.ps1` exit 0。 | 当前 vcxproj，不是 R3 CMake proof。 |
| Debug P1/P2/P3/Converter | filter 实际包含 SourceInspectorTests、ExtractorRealSampleTests、SourceProtocolCleanerTests、NativePreservationObservationAdversarialTests、CleanerValidatePublishContinuityTests、ImageConverterTests、VideoConverterTests；**174/174 pass、0 skip**。 | 包含真实样本，但不声称本轮已做 codec/color 独立 before/after。 |
| Release P2/P3 transaction/ownership | filter 包含 ExtractorTransactionTests、CleanerTrustChainTests、CleanerRealFilesystemTransactionTests、CleanerEighthRoundOwnershipTests；**100/100 pass、0 skip**。 | 真 Windows filesystem/ownership scope，非整个软件全量 suite。 |
| Release ABI/runtime | NativeRuntimeTests **15/15 pass、0 skip**；测试目录 Native DLL 与 Release 构建哈希一致。 | runtime ABI smoke，不替代 59 exports 差分。 |
| GUI Release/package | build exit 0、1 个缺 `mspdbcmf.exe` 的 symbol-package warning、0 errors；真实 MSIX 清单如上。 | warning 不影响当前 R1 产品能力基线；发布签名/安装不是 R1 gate。 |

`designs/各个机型测试/` 本轮重新枚举为 **17 个文件、120,964,218 B**，全部只读。覆盖红米旧 Google V1、Google V2/小米、OPPO/一加、vivo X300 JPEG+GainMap 与旧 JPEG/MP4 双文件、华为 JPEG/HEIC、荣耀、Samsung JPEG/HEIC、Apple JPEG/HEIC+MOV；输入文件逐个 SHA-256 见 TASK evidence。协议 authority 是 `docs/实况照片协议分析文档/` 全部厂商文档和这些真实样本，不重新逆向。

R4–R6 可从这个 corpus 建立 JPEG/HEIC、H264/HEVC、音频和 vendor container 回归；**尚缺**独立 JPEG lossless/metadata before-after corpus、HEIC HDR/GainMap/ICC 色彩对照、libde265/x265 发行许可记录、MF 与最小 libav* 同条件视频 benchmark、目标真机回放。缺口是后续轮次的实际 gate，不用本轮产品 Reader 自验 Writer 冒充独立正确性。样本操作继续在固定 `.ai-tmp/cache/samples/` 和 `.ai-tmp/workspace/`。

## 6. R2–R7 风险与优先级

| 顺位 | 后续行动 | 保护/证明 |
|---|---|---|
| 1 R2 | 以小 PlatformFilesystem + Portable I/O 包住 P1–P3 的 HANDLE/File ID/temp/flush/rename/rollback；核 converter 固定 temp 与 DeleteFileW 入口。 | 不降低 authority/identity/transaction/preservation，也不恢复 Legacy 或 production external CLI。 |
| 2 R3 | 把同一 Native 源、59 export、version、Debug/Release behavior、MSIX runtime 清单转换为 canonical CMake/vcpkg parity。 | 不把旧 CMake generated artifacts 当源配置。 |
| 3 R4 | 建 libjpeg-turbo decode/encode/lossless transform，并测 JPEG 结构/metadata preservation。 | codec 成功不等于 MakerNote/Exif/XMP 保留。 |
| 4 R5 | 建 libheif/libde265/x265 与 auxiliary/HDR Native foundation，决定 WIC/lcms2；显式处理 managed Magick fallback 与丢 GainMap。 | Neutral auxiliary ownership 唯一、Strict/BestEffort outcome 真实。 |
| 5 R6 | MF vs minimal libav* vs hybrid 的视频差分、音频/旋转/时序和硬软策略。 | 用真实样本、独立 probe/device evidence 冻结 Windows P5 backend。 |
| 6 R7 | runtime/capability identity、重复 codec 记录、package feature 用途和 portable-core 独立 build proof。 | P5 不再重建 backend/build architecture。 |

## 7. R1 原始 acceptance matrix

| P4-R1 §8 检查 | 实际证据 | 状态 | 严重性/blocker 与建议 |
|---|---|---|---|
| 主要 capability owner/backend 无空白 | §2 全部列出；未实现的 JPEG lossless/libheif 等明确为缺失，不填假 owner | PASS | 后续 R4/R5 建立，不是 R1 blocker |
| result-affecting 从 C#/C ABI 追到 Native 或明确 legacy | §1 实际 service/API/backend 链，正式 Magick/HDR 和 managed target writer 明确列出 | PASS | managed 正式能力是 R5/P9 风险，不在 R1 越界修复 |
| Windows A/B/C 分类并标记 P1–P3 C | §3 对 WIC/MF、算法/parser、Extractor/Cleaner handle transaction 分别分类 | PASS | R2 mutation site 必须再强制验证身份 |
| external media CLI 代码级结论 | §4 全 Core/GUI/CLI Process.Start/媒体 `.exe` 检索及 helper 实际抛未支持 | PASS | 如新 caller 出现，后续轮次重新审计 |
| Native build/ABI/runtime/package 可比较 | 两配置 Native DLL 哈希、测试加载哈希、59 exports/imports、manifest version、当前 HEAD MSIX 清单/体积 | PASS | R3 做 CMake parity；R7 包体去重 |
| RealSample/benchmark 缺口 | §5 17 个输入文件/哈希、真实测试 scope 与 R4–R6 缺口 | PASS | 后续要求独立验证时缺证据是 FAIL，不能 skip |
| targeted baseline build/test 无未解释失败 | Native/GUI build、174+100+15 targeted tests、全部 0 skip | PASS | 不运行 P0–P10 全功能 suite |
| 后续范围可从 inventory 推导 | §6 风险、轮次和明确技术缺口 | PASS | 仅代表 R1 内部门禁，不宣布 P4 ACCEPT |

本轮本地结论：**READY FOR NEXT P4 SUB-ROUND**。R1 是调查/基线轮，本轮未改 production code 或 tests，也未开始 R2。`.ai/project-facts.md` 在恢复时被错误填成截断的 Python 补丁，不能作为工程事实；本轮从源码/构建取证。Doctor `-ActiveOnly` 检查中 core codegraph/validator 可用，但恢复配置缺 `.agents/mcp_config.json`，属于本轮未使用的可选平台 adapter 缺口；不冒充产品测试失败，也不手工改生成平台副本。
