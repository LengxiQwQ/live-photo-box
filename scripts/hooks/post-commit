#!/bin/sh
# Git post-commit hook for Live Photo Box (Project Scoped & Completely Silent)
# Runs only when a git commit is made inside this repository.

powershell.exe -NoProfile -ExecutionPolicy Bypass -WindowStyle Hidden -File scripts/package-project.ps1 -OnlyIfChanged >/dev/null 2>&1 &

exit 0
